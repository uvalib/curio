# run application

# run from here, since application expects web template in web/ and writes pdfs to tmp/
cd bin; ./curio -tracksys $TRACKSYS_URL -apollo $APOLLO_URL -iiif $CURIO_IIIF_MAN_URL -fedora $WSLS_FEDORA_URL -host $CURIO_HOST

#
# end of file
#
